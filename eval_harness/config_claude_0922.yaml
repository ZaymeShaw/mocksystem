# Additive Claude config for shared 0922 — does NOT replace default config.yaml
# (in-flight Claude dual runs keep using config.yaml / their own --claude-config).
profile_path: profiles/claude_code_mock_system.yaml
bundle_path: bundles/120_prompt_only_0922_shared.jsonl
dataset_path: data/120题-仅问句版-0922.md
gold_dataset_path: data/复杂协同与上下文问题集-120题(2).md
