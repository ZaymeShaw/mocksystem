# Runner entry config (thin): points at Bundle + Profile.
# Dataset markdown is NOT read by the runner; import it into a Bundle first.

profile_path: profiles/claude_code_mock_system.yaml
bundle_path: bundles/120_prompt_only_v1.jsonl

# Optional overrides (win over profile if set)
# claude_bin: /Users/xiaozijian/.npm-global/bin/claude
# eval_runs_dir: eval_runs

# Legacy source (import only; runner ignores)
dataset_path: data/120题-仅问句版(1).md
gold_dataset_path: data/复杂协同与上下文问题集-120题(2).md
